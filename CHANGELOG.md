# Changelog

本项目使用严格顺序版本 `1.0.N`。每次推送功能、修复、配置或文档更新到 GitHub，都必须把最后一位恰好增加 1，不得跳号。

## [1.0.41] - 2026-08-20

### Changed

- 买家端 Telegram 展开逻辑加固：`ready()` 后**立即执行 `expand()`**，并在 300/800/1500/3000/6000ms 多个时间点重试，`isExpanded` 已展开后自动停止，覆盖慢速 webview / 不同客户端就绪速度，最大限度避免 Mini App 打开后停留在 Bottom Sheet 半屏。
- 新增 `fullscreen_changed` 事件监听：视口变化（用户手动下拉、键盘弹出）或退出全屏时再次 `expand()`，确保 Mini App 始终处于最大可用高度。
- 全屏（`requestFullscreen`）首次手势失败后自动重新挂载监听，允许后续手势重试，避免首次触摸发生在 SDK 尚未就绪时导致全屏永远无法进入；失败静默不阻断页面。
- 买家端顶部 Header 重设计：从白底边框"框框"改为品牌渐变（靛蓝 → 紫）+ 柔光高光 + 毛玻璃品牌徽标，首页展示品牌锁标，订单/我的页展示对应标题与副标题，整体更精致统一。
- 订单详情页信息补全：
  - 实付金额改为突出展示（大号金额 + 货币单位），商品、规格、数量、单价完整呈现。
  - 支付信息新增**收款地址**展示（等宽字体 + 一键复制）。
  - 订单信息新增**发货状态**（等待发卡 / 发卡处理中 / 已发卡 / 发卡失败）与**支付时间**。
  - 卡密信息标题显示卡密张数（如"卡密信息（3 张）"）。
- 订单列表行显示规格与购买数量（如"标准版 × 2"），信息更完整。
- 个人中心余额卡片改为独立可点击的渐变充值卡片（整卡可点击进入充值），与充值页余额卡视觉统一。
- 空状态图标升级为柔和渐变圆角底；二级页面顶部栏增加柔和阴影，与渐变主 Header 风格一致。

### Fixed

- 修复部分 Telegram 客户端 / 慢速 webview 下单次延迟 `expand()` 被吞掉、Mini App 打开后仍停留在半屏的问题。
- 订单详情支付时间在无支付对象时也能正常展示（移入订单信息区，避免遗漏）。

## [1.0.40] - 2026-08-19

### Added

- **站点对接 OpenAPI**（参考 Dujiao-Next 协议）：HMAC-SHA256 签名（`{METHOD}\n{PATH}\n{TIMESTAMP}\n{MD5(BODY)}`，±60 秒容差）；API 凭证管理（Key/Secret 加密存储，仅创建时展示一次）；对接连接管理（测试连通 / 拉取上游商品 / 创建采购单 / 处理上游回调，含回调验签）
- **钱包退款入账**：管理端可将已支付订单退款至买家余额，写 `refund` 类型流水，幂等防重复
- 数据库迁移 v8：`api_credentials`、`upstream_connections` 表

### Changed

- 订单详情页重构为 5 个信息卡片（状态 / 商品含单价数量 / 支付含网络代币与截止时间 / 卡密含密码 / 订单编号可复制），补全关键信息展示
- 本地开发调试登录恢复：无 Telegram initData 时走 `/api/auth/dev`，生产环境仍返回 404（安全隔离）

### Fixed

- 修复 Telegram Mini App 打开后停留在 Bottom Sheet 半屏、需要用户手动展开的问题：`ready()` 后延迟 **300ms** 执行 `expand()`，并监听 `viewportChanged` 事件再次展开；失败静默不阻断页面；fullscreen 独立于 expand 保留手势触发

## [1.0.39] - 2026-08-19

### Fixed

- 修复 Telegram Mini App 打开后停留在 Bottom Sheet 半屏、需要用户手动展开的问题：
  - `ready()` 后延迟 **300ms** 再执行 `expand()`，避免在 Telegram 尚未就绪时过早请求展开被忽略。
  - 新增 `viewport_changed` 事件监听：视口变化（用户手动下拉、键盘弹出、全屏切换等）时再次 `expand()`，确保 Mini App 始终展开到最大可用高度。
  - `expand()` 失败静默忽略，不阻断页面渲染与交互。
  - fullscreen（`requestFullscreen`）逻辑与 `expand()` 完全独立，仍保留首次手势（`pointerdown`）触发，互不干扰。

## [1.0.38] - 2026-08-19

### Changed

- 买家端接入 Telegram 原生 **BackButton**：首页（商城 / 订单 / 我的）隐藏，二级页面（商品详情 / 订单详情 / 充值 / 余额明细）显示，点击触发与页面返回按钮一致的返回逻辑。
- 订单页交互简化：移除顶部手动刷新按钮；进入订单页自动刷新订单列表，切换订单分类（全部 / 待支付 / 进行中 / 已完成 / 已关闭）时也自动重新拉取。
- 移除底部导航"订单"标签上的订单数量角标，底部导航保持简洁。
- 移除所有开发演示入口：删除 `/api/auth/dev` 调试登录接口与前端 dev 登录兜底，买家端只接受 Telegram `initData` 验签登录；删除"模拟用户"标记、开发提示条及对应样式。
- 视口与安全区适配保持官方规范：不依赖 `100vh`，统一使用 `--tg-viewport-stable-height` 与 `--tg-viewport-content-safe-area-inset-bottom`。

### Removed

- `/api/auth/dev` 开发登录接口（生产环境原本即返回 404，本次彻底移除）。
- 底部导航订单角标、订单页刷新按钮、"模拟"开发标记与提示条。

## [1.0.37] - 2026-08-19

### Changed

- 买家端重新接入 Telegram **全屏模式**（`requestFullscreen`）：监听 `pointerdown`（手指按下瞬间）即请求全屏，`click` 作为键盘/无障碍操作兜底，用户"一碰屏幕就进全屏"；旧客户端 / Telegram 网页版检测不到 `requestFullscreen` 时自动降级为原有 `expand()` 展开行为。
- 保留 Telegram 原生顶栏和自定义底部 Tab 导航结构不变；全屏失败（用户取消或环境不支持）静默忽略，保持当前状态。
- 说明：Telegram 官方限制 `requestFullscreen` 必须由用户手势触发，无法实现"打开即全屏、零交互"；若需每次打开自动全屏，可在 Telegram 客户端内开启小程序的"全屏模式"偏好（前提：客户端为 2024-12 之后的新版本）。

## [1.0.36] - 2026-08-19

### Added

- 买家端新增**余额支付**：下单时可选“余额支付”，余额充足直接扣款并进入发卡队列，余额不足提示充值；服务端支持 `paymentMethod: 'balance'`，扣款写入余额流水（`balance_entries`）并幂等。
- 商城首页与商品详情新增支付方式展示：公共配置返回 `paymentMethods` 列表（当前为 DujiaoPay USDT），买家端默认选中可用方式。
- 个人中心“余额明细”与充值入口收敛到 `/wallet` 钱包路径：`/wallet/recharge` 充值页、`/wallet/balance` 余额明细页，SPA 路由直接支持钱包子路径。
- 充值接口支持显式 `provider` 参数，未传时默认 DujiaoPay，传了不支持的提供商会返回 `invalid_payment_provider`。

### Changed

- 订单创建接口改为仅在非余额支付时校验支付渠道就绪状态；余额支付无需等待支付渠道即可下单。
- 余额支付成功后立即入队发卡任务，与 Webhook 异步发卡共用同一幂等队列。

### Fixed

- 修复买家端在浏览器直接打开（无 Telegram initData）时无法预览的问题：开发环境保留 `/api/auth/dev` 调试登录兜底，生产环境仍返回 404，不提供开发登录。

### Changed
- 个人页余额明细改为按需打开的独立面板，不再常驻显示
- 新增"余额明细"菜单项，点击后打开面板查看完整流水记录

### Fixed
- 修复个人页余额流水默认展示导致页面过长的问题

## [1.0.34] - 2026-08-19

### Changed

- 买家端迁移到 React + TypeScript + TailwindCSS，通过项目兼容别名 `@telegram-mini-apps/sdk-react` 使用官方包 `@telegram-apps/sdk-react` 管理 Telegram Mini App 生命周期。
- 按官方要求改为 `ready()` 后 `expand()`，禁用垂直下拉收起并隐藏原生 `BackButton`；保留 Telegram 原生顶部栏和自定义底部 Tab 导航，移除 `requestFullscreen()`。
- 使用 Telegram viewport CSS 变量与 `env(safe-area-inset-*)` 处理刘海、底部手势区和动态 viewport；生产镜像增加 React 前端构建阶段。

## [1.0.33] - 2026-08-19

### Changed

- 买家端商城首页重新设计：品牌标记与名称收敛为顶部居中组合，分类与商品目录层级更紧凑，空商品状态改为全宽可操作面板。
- 订单页在零订单、加载中和有订单时均显示状态分类筛选；刷新订单按钮缩小为独立图标按钮，避免遮挡标题和 Telegram 原生控件。
- 空商品状态新增目录刷新操作，目录变更后可直接重新拉取商品列表。

## [1.0.32] - 2026-08-19

### Changed

- 将 v1.0.31 的 Telegram 顶部安全区与标题居中修复收敛到主线发布版本。
- 清理过期发布说明和已完成路线图事项，仓库统一采用 `main` 作为唯一长期开发与发布分支。

## [1.0.31] - 2026-08-19

### Fixed

- 修正 v1.0.30 在部分 Telegram iOS 客户端未返回 `contentSafeAreaInset` 时仍被原生关闭控件遮挡的问题。
- Telegram 环境下为顶部标题栏保留最小原生控件避让间距；商城、订单、我的标题改为独立居中，订单刷新按钮不再挤偏标题。

## [1.0.30] - 2026-08-19

### Fixed

- 修复 Telegram Mini App 全屏模式下，原生顶部关闭栏 / 菜单栏遮挡商城、订单和个人中心标题的问题。
- 接入 Telegram 官方 `contentSafeAreaInset`，并在视口、全屏和安全区变化时动态同步顶部安全区；标题栏保持在安全区内并垂直居中。


## [1.0.29] - 2026-08-19

### Changed

- 后台现有功能完善（对照发卡网常规逻辑，不新增模块）：
  - **订单记录**：支持按状态筛选（待支付 / 确认中 / 已支付 / 发卡中 / 已完成 / 发卡异常 / 已取消），后端 `listAdminOrders` 支持 `statuses` 过滤参数，前端新增胶囊筛选栏。
  - **用户管理**：补上余额流水查看入口（复用已有 `GET /api/admin/users/:id/balance-entries` 接口），新增「流水」按钮 + 弹窗展示变动明细（金额 / 变动后余额 / 类型 / 备注 / 时间）。
  - **分类管理**：补齐编辑与启停能力（此前只能创建）。新增 `PATCH /api/admin/categories/:id`（名称 / slug / 排序 / is_active），前端分类行加「编辑」「停用/启用」按钮；停用分类后商品保持原样、买家端归类为未分类。
  - **卡密导入**：导入后提示实际结果统计（成功 X 条，跳过重复 Y 条），替代原先笼统的"已导入"提示（数据后端早已返回，仅前端未展示）。

## [1.0.28] - 2026-08-19

### Changed

- 全屏触发时机优化：改为监听 `pointerdown`（手指按下屏幕的瞬间）即请求全屏，用户"一碰屏幕就进全屏"，感知上接近自动；保留 `click` 监听作为键盘/无障碍操作兜底，并加去重标记防止重复请求。
- 说明：Telegram 官方限制 `requestFullscreen` 必须由用户手势触发，无法实现"打开即全屏、零交互"；若需每次打开自动全屏，可在 Telegram 客户端内开启小程序的"全屏模式"偏好（前提：BotFather 已启用 Fullscreen 能力）。

## [1.0.27] - 2026-08-19

### Added

- 接入 Telegram 官方 **Mini App 全屏模式**（`requestFullscreen` API）：
  - 小程序打开后，用户首次点击页面即请求进入全屏（官方要求必须由用户手势触发），全屏后 Telegram 原生顶栏/底栏收起，仅显示 Mini App 内容。
  - 旧客户端 / Telegram 网页版自动降级：检测不到 `requestFullscreen` 时保持原有 `expand()` 展开行为，不影响正常使用。
  - 全屏失败（用户取消或环境不支持）静默忽略，保持当前状态。
- 注意：需要在 BotFather 中为该 Bot 的小程序启用 Fullscreen 能力，且客户端需为 2024-12 之后的新版本。

## [1.0.26] - 2026-08-19

### Changed

- 移除商城首页顶部的支付状态公告条（"USDT · TRON · 自动发卡"），首页从上到下更紧凑：分类胶囊直接置顶，商品更快可见。

## [1.0.25] - 2026-08-19

### Changed

- 商城首页按反馈细化：
  - 移除搜索框（保持首页简洁，不占用首屏高度）。
  - 分类改为**单行胶囊标签**（椭圆白底描边，激活态渐变填充 + 数量徽标），替代原先别扭的双行图标卡。
  - 双列商品卡**整体压矮**：图片 4:3 改为 16:10，标题/描述/内边距收紧，描述单行截断，购买键 40px 缩至 36px，单位屏可容纳更多商品。

## [1.0.24] - 2026-08-19

### Changed

- 商城首页**结构性重构**（不再只是换肤，布局与信息架构重做）：
  - 移除占首屏一半的巨型 Banner，改为 52px 细公告条（品牌标 + 支付状态一句话）。
  - 新增商品搜索框：按名称/描述/分类即时过滤，只刷新商品网格、不重建整页（输入不丢焦点）。
  - 商品卡从"左图右文横条"改为**上图下文竖卡**，移动端双列货架式布局（桌面端三列），图片 4:3 通栏大图。
  - 价格与库存移到卡片底部（价格大字 + 圆形渐变购买键），视觉重点更清晰。
  - 列表页移除规格下拉，规格选择收敛到商品详情页，列表更干净、购买路径更聚焦。
- 保留 v1.0.23 晨曦亮色视觉与 v1.0.20 动效层（Dock、骨架屏、入场动画全部沿用）。

## [1.0.23] - 2026-08-19

### Changed

- 视觉风格全面改为「晨曦 Lumina」明亮系，**弃用 v1.0.21 暗夜流光**（整层移除）：
  - 浅雾蓝底色（`#f3f5fb`）+ 柔光光晕，主色为宝蓝→亮紫渐变（`#4f6bf8 → #8b5cf6`），白色玻璃卡片 + 柔和阴影。
  - **底部导航全新设计**：改为悬浮胶囊 Dock（圆角 24px 白色毛玻璃胶囊 + 渐变滑块 + 激活项白字），不再贴底长条；桌面端 Dock 居中收窄。
  - Banner、Logo、主按钮、分类激活态统一渐变 + 轻辉光；价格暖橙高亮；状态标签按语义配色。
  - 商品卡、订单、抽屉、面板、支付/充值、个人中心全部亮色玻璃化；二维码白色高对比。
  - 后台同步：浅雾蓝底 + 白卡 + 渐变按钮，侧边栏保留品牌深蓝渐变；登录页与弹窗同步调整。
  - Telegram 原生顶栏/背景跟随浅色（`#f3f5fb`）。
- 修复：上一版暗夜层残留的初始化页闪白问题由新亮色层完整接管。

## [1.0.22] - 2026-08-19

### Fixed

- 修复「暗夜流光」重构的最后一处视觉残留：买家端初始化加载页与连接失败页（`.page`/`.empty` 结构）补上暗色适配，打开小程序不再闪白。

## [1.0.21] - 2026-08-19

### Changed

- 买家端与后台整体视觉重构为「暗夜流光」（Aurora Dark）风格：
  - 墨夜底色（`#0b0d13`）+ 靛紫→流光蓝主色渐变（`#7c5cff → #3b9df8`），与旧版 iOS 白底蓝字彻底区分。
  - 页面背景加入霓虹光晕（紫/蓝/粉三色径向渐变）与星尘点缀，卡片统一深色半透明玻璃质感 + 细边框 + 悬停微光。
  - 商城 Banner、品牌 Logo、底部 Tab 滑块、主按钮全部改为品牌渐变 + 辉光投影；价格使用暖橙高亮。
  - 分类胶囊、状态标签、订单徽章按语义重新配色（青=进行中、金=待支付、绿=成功、红=异常）。
  - 商品详情、支付/充值页、个人中心、抽屉与订单面板全部同步深色玻璃化；二维码区域保持白色高对比，扫码识别不受影响。
  - 骨架屏、空态、Toast 适配暗色。
  - 固定深色品牌风格：Telegram 原生顶栏/背景色跟随暗夜底色（`setHeaderColor`/`setBackgroundColor`），浅色主题下不再漂白撕裂。
- 后台同步重构：深空渐变侧边栏、暗色毛玻璃顶栏、表格/表单/卡片深色化、按钮渐变辉光、支付控制台暗夜渐变卡。

## [1.0.20] - 2026-08-19

### Changed

- 买家端整体交互丝滑化（Smooth motion layer）：
  - 修复全量重渲染导致的滚动跳动：分类/规格切换、改数量等操作后页面滚动位置不再丢失，长列表操作不跳顶。
  - 支付/充值轮询不再每 4~5 秒重建整个页面与二维码：订单面板只做局部刷新并保留滚动位置，二维码 SVG 内容未变时直接复用，扫码页更稳定。
  - 购买抽屉（bottom sheet）与订单面板改为带过渡的滑入/滑出（抽屉上滑 + 遮罩淡入淡出，订单卡片弹入），不再瞬间闪现。
  - 统一按压反馈：所有可点元素（卡片、分类、按钮、订单行、菜单项等）点击时轻缩放，移动端消除默认 tap 高亮闪块；接入 Telegram HapticFeedback 轻触/选中震动，手感更跟手。
  - 首次加载与分类切换时商品卡片、分类胶囊、Banner 分批错峰入场（stagger），打开商品详情有分层进入动画；日常操作不重复播放动画，避免动画轰炸。
  - 订单加载态由转圈改为骨架屏（shimmer），视觉更平稳。
  - 余额数字变化时平滑滚动（ease-out 数字过渡），充值/我的页同步生效。
  - 商品图片懒加载（`loading="lazy"` + `decoding="async"`），首屏更快。
  - 离开充值页时停止充值轮询与倒计时，避免后台持续重绘。
  - 所有新增动画遵守 `prefers-reduced-motion`，系统开启“减少动态效果”时自动关闭。
- 管理后台交互丝滑化：视图切换轻量淡入、表单面板展开动画、调整余额弹窗淡入弹出（自动聚焦金额输入框）、按钮按压反馈、表格行 hover 高亮、卡片悬停微浮起（仅桌面端）。
- `npm test` 脚本改为跨平台写法（`node --test "test/*.test.js"`），不再依赖 bash 的 for 循环，Windows/macOS/Linux 均可直接运行。

## [1.0.19] - 2026-08-19

### Changed

- 买家端整体 UI 重构为原生苹果（iOS）风格：
  - 全界面接入 Telegram `themeParams` 动态主题变量（页面底色、卡片、文字、主色、分隔线、毛玻璃底色），明/暗主题下自动适配。
  - 导航栏与底部 Tab Bar 改为 iOS 毛玻璃（`saturate(180%) blur(24px)` + hairline 分隔线）；Tab Bar 贴底全宽。
  - 商品卡、订单卡、个人中心、菜单、面板改为 iOS 分组卡片样式（白底 + 轻阴影/hairline + 系统圆角）。
  - 分类筛选、状态标签改用 iOS 标签风格（主色淡填充 + 主色描边）。
  - 主按钮、输入框、抽屉/订单面板改为 iOS 控件风格（系统主色、iOS sheet）。
- 底部导航切换更顺滑：统一使用吸顶缓动曲线（`.32,.72,0,1`），视口高度与滑动位置同步过渡；切换不再依赖单一 `requestAnimationFrame`，改为强制 reflow 提交初始位置后同步更新目标值，后台/省电场景下滑块不再卡在原位。
- 激活 Tab 图标改为 iOS 风格色彩切换（主色，无底色块、轻缩放上浮反馈）。
- 更换订单图标：原纸质回执样式升级为圆润的“订单卡片 + 对勾徽章”，底部导航 Tab、订单列表项、“我的”订单入口保持一致。

### Added

- 订单页面新增状态分类筛选：“全部 / 待支付 / 进行中 / 已完成 / 已关闭”，带各分类订单数角标，切换分类即时过滤。
- 订单分类无匹配时显示“该分类下暂无订单”友好空态，而非误导性的“还没有订单”。

### Fixed

- 修复订单筛选“全部”分组计数始终为 0 的问题（`filterOrders` 未处理无状态集的“全部”分组）。
- 修复订单分类筛选后重进订单页状态丢失的问题（分类选择持久化到全局 state）。
- 订单列表状态改用彩色标签（待支付=橙、进行中=蓝、已完成=绿、需处理/退款=红、已关闭=灰），一眼区分订单状态。

## [1.0.18] - 2026-08-18

### Changed

- 买家端底部导航改为横向滑动切换，选中按钮增加同步滑动指示层。
- 主页面切换保留商城、订单和个人中心的状态，订单数据异步加载不再阻断切换动画。

## [1.0.17] - 2026-08-18

### Fixed

- 修复后台“商品与规格”页面创建分类后看不到分类的问题：该页面新增“全部分类”列表，创建的分类会立即显示（名称、Slug、商品数、启用/停用状态）。
- 后台分类列表接口补充每个分类的商品数量统计。

## [1.0.16] - 2026-08-18

### Added

- 余额充值接入 DujiaoPay 支付：买家在充值页输入金额后出示收款二维码，扫码付款到账后余额自动增加。
- 新增充值单与充值支付流水（数据库迁移 v7），充值单号使用 `RC` 前缀与商品订单区分。
- 新增充值相关接口：`POST /api/me/recharge`（发起充值）、`GET /api/me/recharge/:no`（轮询状态）。
- Worker 增加充值对账，超时支付自动确认或标记过期。

### Changed

- “我的”页面个人信息胶囊优化：头像放大、上下渐变背景更像胶囊、文本各行左边缘对齐、余额与身份信息合并为一个胶囊。
- “已连接”状态前增加绿色呼吸闪烁状态点，未连接时显示红色状态点。

### Security

- 充值到账采用原子锁定与幂等入账，重复支付回调不会重复增加余额。
- 充值支付（RC）与商品订单发卡（XX）在 Webhook 分发上完全隔离。

## [1.0.15] - 2026-08-18

### Changed

- 买家端“我的”页面：账户余额与个人信息合并进同一个胶囊卡片，余额与充值按钮整合在胶囊底部，充值按钮位于右侧。
- 个人信息胶囊内已无头像占位字母，无用户名时不保留空行，身份信息（姓名、用户名、Telegram ID：）紧凑排列。
- 移除胶囊顶部英文小标（TELEGRAM ACCOUNT）。

## [1.0.14] - 2026-08-18

### Added

- 用户新增余额字段与余额流水表（数据库迁移 v6），支持充值、调整、消费、退款等余额变动记录。
- 买家端“我的”页面胶囊下方新增账户余额卡片与“充值”按钮，并提供余额充值页（当前余额、充值金额、余额明细）。
- 管理后台用户管理新增“余额”列，可对指定用户调整余额并填写备注，调整记录写入审计日志。
- 新增余额调整接口：后台 `PATCH /api/admin/users/:id/balance`、`GET /balance-entries`，买家 `GET /api/me/balance`。
- 新增余额调整回归测试与数据库迁移 v6 相关更新。

### Changed

- 买家端个人信息胶囊移除头像内首字母、无用户名时不保留空行，Telegram ID 文案改为“Telegram ID：”。
- 管理后台全新代码级重构：侧边栏与顶栏去除英文装点文案（Workspace / Operations console 等含糊“AI 味”措辞），改为简洁直白的功能说明。

## [1.0.13] - 2026-08-18

### Added

- 商品与分类创建/编辑时，Slug 留空会自动按名称生成：名称转为小写字母数字连字符，同名自动追加 `-2`、`-3` 等后缀，纯中文名称使用随机尾号兜底，保持全局唯一。

### Changed

- 后台管理员账号密码最低长度从 12 位放宽到 8 位，服务端校验与后台提示文案同步更新。

## [1.0.12] - 2026-08-18

### Changed

- “我的”页面顶部不再显示副标题“账号与服务”，只保留标题“我的”。
- 个人信息胶囊卡整体放大：更大头像、更宽内边距和更大字号，昵称、用户名、@与 Telegram ID 阅读更清晰。
- “我的”页面胶囊内补回 Telegram ID 显示，格式为 “Telegram ID · <id>”。

## [1.0.11] - 2026-08-18

### Changed

- 买家端首页全新视觉：使用浅灰紫底色、蓝紫主色与更大圆角，重构 Hero 横幅、商品卡片和整体层次。
- 商品分类改为可横向滚动的胶囊式分类，带序号标识和各分类商品数量。
- 个人信息改为整块胶囊卡，集成头像、昵称、用户名与“已连接”状态。

### Removed

- 移除买家端搜索框、顶部搜索图标及与搜索相关的状态、事件和过滤逻辑。

## [1.0.10] - 2026-08-18

### Changed

- 买家端底部导航改为悬浮胶囊式布局，增加独立图标底板、清晰选中态、订单角标和点击反馈。
- 优化移动端安全区与内容底部留白，避免悬浮导航遮挡商品内容。
- 统一顶部工具按钮、商城 Banner、商品卡片的蓝色主题、边框和阴影层次。

## [1.0.9] - 2026-08-18

### Added

- 后台新增“支付渠道”页面，可配置和暂停 DujiaoPay 的 API 地址、Key ID、API Secret、Webhook Secret、支付网络、币种与订单有效期，并可复制 Webhook 地址和测试凭据连接。
- 买家端新增商品搜索、可筛选商城，以及独立商品详情路由 `/products/:slug`；详情页展示封面、规格、库存、销量、商品说明和固定购买栏。
- 新增支付渠道状态回归测试，覆盖“已配置但暂停新订单”和“未配置”状态。

### Changed

- DujiaoPay 支付配置支持环境变量初始化或后台加密保存；保存后 API 立即刷新，Worker 在对账周期同步最新配置。
- 支付渠道暂停时拒绝新订单，但保留已有订单的 Webhook、查单、取消和自动发卡处理。
- 买家端商城重做为更紧凑的移动购物流，购买按钮会准确反映支付渠道配置、暂停和库存状态。

### Security

- DujiaoPay API Secret 和 Webhook Secret 使用 AES-256-GCM 加密存储，设置接口与后台页面不回显明文。
- 多 API/Worker 进程在关键支付操作前重新读取数据库配置，避免继续使用陈旧支付凭据。

## [1.0.8] - 2026-08-18

### Fixed

- 后台 Telegram Bot Token 保存后显示明确的成功提示、数据库来源和精确保存时间。
- 后台会话过期时保存操作直接跳回登录页，不再停留在设置页表现为“点击无反应”。
- 买家旧 Bearer 会话失效时，服务端可通过受保护请求携带的 Telegram `initData` 重新验签恢复身份，不再显示"登录状态已失效"。

### Changed

- 所有买家受保护请求自动附带当前 Telegram `initData`，使服务端能在会话重放阶段按实际需要恢复身份。
- 后台 API 返回 Telegram Bot Token 配置状态、数据库来源和最新更新时间。

### Security

- 后台会话失效与买家会话失效在错误码和响应上保持明确区分，便于前端正确处理登出逻辑。

## [1.0.7] - 2026-08-18

### Changed

- 运行时支付固定使用真实 DujiaoPay，不再支持 Mock 支付配置。
- 买家端只接受 Telegram `initData` 登录，移除开发账号登录、`devUser` 回退和模拟付款按钮。
- 服务端移除开发登录 API、模拟付款 API 和 Mock 付款页面路由；开发环境也不会自动创建演示用户、商品或卡密。
- 种子数据改由独立后台管理员导入，不再伪造 Telegram 管理员身份。

### Fixed

- 买家端打开时清除旧的本地会话，使用当前 Telegram 身份重新建立会话；接口返回 `401` 时仅使用真实 `initData` 续签并重试一次。
- 未从 Telegram Mini App 打开时显示明确的重新打开提示，不再静默创建本地用户。

### Security

- `PAYMENT_PROVIDER=mock` 会在启动配置校验阶段直接拒绝。
- 旧数据库中的未完成 Mock 订单会被迁移为已关闭并释放预留卡密；旧数字 Telegram 管理员审计记录会隔离为 `admin:legacy:` 标识，后续 Telegram 登录按普通买家创建，独立后台管理员保持不变。
- 新增真实支付配置和旧订单迁移回归测试。

## [1.0.6] - 2026-08-18

### Fixed

- 买家会话失效或服务重启后，Mini App 自动使用 Telegram `initData` 重新登录并重试原请求，避免下单时持续提示登录失效。
- 每次打开 Mini App 都同步最新 Telegram 资料，保存并展示真实头像、完整姓名和用户名。
- “我的”页面将冗长的 `Telegram ID` 文案缩短为 `ID:`，未设置用户名时显示明确占位说明。

### Added

- 管理后台新增用户管理页，可按姓名、用户名或 Telegram ID 搜索买家。
- 用户列表展示头像、Telegram ID、语言、账号状态、订单数、已支付订单数、累计消费和最近下单时间。
- 管理员可停用或恢复买家账号；停用不会删除历史订单、付款或卡密记录。

### Security

- 已停用用户不能重新建立 Telegram 会话或执行买家操作，状态变更写入审计日志。
- Telegram 头像地址只接受 HTTPS，加载失败自动回退到姓名首字母。

## [1.0.5] - 2026-08-17

### Changed

- 管理后台改为独立网页账号密码登录，不再依赖 Telegram WebApp 或 `initData`。
- 后台新增系统设置页面，可配置 Telegram Bot Token 并修改管理员账号密码。
- Telegram Bot Token 使用 AES-256-GCM 加密存储，后台只显示配置状态，不回显明文。
- 管理员密码使用带随机盐的 `scrypt` 哈希存储，管理员会话与买家会话分离。
- 首个管理员通过 `ADMIN_USERNAME` 和 `ADMIN_PASSWORD` 环境变量初始化，后续可在后台修改。
- 新增管理员账号与加密设置数据库迁移，以及独立认证和密文存储测试。

### Security

- 后台 API 仅接受独立管理员会话，Telegram 买家会话不能访问；买家端不显示管理后台入口。
- 管理员登录增加独立 IP 速率限制，错误响应不区分账号不存在或密码错误；任何环境都不生成默认管理员密码。

## [1.0.4] - 2026-08-17

### Changed

- Telegram Mini App 创建支付订单后直接打开站内付款页，不再自动跳转 DujiaoPay 第三方收银台。
- USDT 付款页原生展示二维码、精确应付金额、TRON 网络、收款地址、订单号和服务端校准倒计时。
- 付款页每 5 秒轮询订单状态，支持付款确认、链上确认、自动发卡和订单过期状态实时切换。
- 支付适配器新增统一内嵌二维码协议，为后续 EPUSDT、易支付微信/支付宝等渠道复用同一付款界面。
- 新增支付展示字段数据库迁移；旧 DujiaoPay USDT 待支付订单可从已有地址恢复内嵌付款信息。
- 新增本地二维码生成组件，二维码内容不经过第三方图片接口，支付渠道密钥和原始响应不会下发到前端。

### Security

- 新支付渠道若只返回外部收银台网址、没有可验证的内嵌二维码信息，服务端拒绝创建付款会话，防止静默回退到第三方页面。
- 付款成功仍仅以服务端验签 Webhook 或服务端查单为准，浏览器轮询只负责状态展示。

## [1.0.3] - 2026-08-17

### Changed

- 买家端重构为 Telegram 原生小程序结构，新增固定的商城、订单、我的底部标签栏。
- 订单从弹层入口改为独立页面，支持原生空状态、数量角标、下拉式刷新入口和订单列表。
- 新增我的页面，集中展示 Telegram 账号、售后入口、管理后台和运行版本。
- 商城改为紧凑横向商品卡，价格、库存、规格和购买操作在手机首屏直接可见。
- 支付确认改为移动端底部 Sheet，订单详情保留独立全屏查看与卡密复制能力。
- 适配 Telegram 顶部与底部安全区域，并同步原生背景色和触感反馈。

## [1.0.2] - 2026-08-17

### Changed

- 全面重做买家端视觉系统：改为蓝灰工作台背景、深靛首屏、清晰的商品卡层级和更紧凑的移动端首屏。
- 全面重做管理后台视觉系统：改为深靛侧栏、白色工作区、统一 KPI 卡片、表格、表单和操作按钮。
- 优化手机端导航、商品卡、订单面板、空状态、后台表单和 SKU 编辑行布局。
- 统一按钮、图标、边框、阴影、状态标签和响应式断点，修复桌面/移动端比例不一致。
- 版本资源缓存、主题色和运行时版本显示同步至 `1.0.2`。

## [1.0.1] - 2026-08-17

### Changed

- 版本号固定为 `1.0.N` 严格顺序递增。
- 新增功能、重大更新、修复、配置和文档更新都只增加最后一位。
- 自动版本检查只接受上一个标签加 1，禁止跳到 `1.1.0`、`2.0.0` 或跨号发布。

## [1.0.0] - 2026-08-17

### Added

- Telegram Mini App 买家端与 Telegram 管理员登录。
- 商品、分类、SKU、加密卡密库存、订单和自动发卡管理。
- DujiaoPay HMAC 建单、查单、取消与 Webhook v1 验签适配器。
- 支付幂等、库存预留、Webhook 去重、失败重试和异步发卡任务。
- 管理后台数据看板、商品与 SKU 编辑、卡密导入、订单和支付回调异常视图。
- SQLite 版本化迁移、WAL 在线备份和 DujiaoPay 凭据自检命令。
- Docker Compose API/Worker 编排、非 root 镜像与健康检查。
- 支付、Webhook、退款登记、过期卡替换和 Worker 恢复测试。
- 语义化版本、变更日志、发布校验、运行时版本展示和 `v1.0.0` 发布标签。

### Security

- AES-256-GCM 卡密加密和 HMAC 指纹去重。
- Telegram `initData` 服务端验签、会话签名、管理员白名单和接口限流。
- 支付金额、币种、链、代币、订单号及原始 Webhook body 校验。
- 生产 HTTPS、官方 DujiaoPay 域名、可信代理和静态资源 CSP 约束。

### Fixed

- 修复 Worker 崩溃后任务永久锁定、定时任务重叠和退出时中断任务。
- 修复预留卡过期后仍可能发放、迟到支付库存不足和幂等键串单。
- 修复后台隐藏表单默认展开、按钮图标尺寸、移动空状态和旧资源缓存。
- 修复 Node 22 实验性 SQLite 并行测试进程偶发崩溃，测试文件改为串行执行。
