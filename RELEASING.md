# Release Process

XiuXian 当前版本为 `1.0.5`。所有推送到 GitHub 的项目更新都必须使用新版本号，不能在同一版本下追加提交。

## 版本规则

版本固定使用 `1.0.N`，只允许最后一位严格递增：

```text
1.0.0 -> 1.0.1 -> 1.0.2 -> 1.0.3 -> 1.0.4 -> 1.0.5
```

- 每次更新只能在上一个已发布版本的最后一位加 `1`。
- 新增功能、重大更新、修复、文档、配置、样式和运维改进都遵守同一规则。
- 不使用 `1.1.0`、`2.0.0` 等版本，也不允许从 `1.0.1` 跳到 `1.0.3`。
- `scripts/check-version.js` 会根据最新 Git 标签计算唯一允许的下一个版本。

## 每次发布必须完成

1. 确认远程 `main` 没有未合并更新，并拉取全部 `v1.0.*` 标签。
2. 将 `package.json` 的最后一位在上一个标签基础上加 `1`。
3. 同步 README、前端资源参数、运行时回退值和 Docker 镜像版本。
4. 在 `CHANGELOG.md` 顶部新增该版本及具体更新内容。
5. 运行 `npm run version:check`、`npm test` 和必要的 HTTP/浏览器验收。
6. 使用包含版本号的提交说明，例如 `release: v1.0.2`。
7. 创建带说明的 Git 标签，例如 `git tag -a v1.0.2 -m "XiuXian v1.0.2"`。
8. 推送分支和对应标签。
9. 交付回复必须明确标注：版本号、更新内容、提交哈希、标签和测试结果。

## 禁止事项

- 不得修改已经发布标签指向的提交。
- 不得跳号、改变 `1.0` 前缀或在未更新版本号和变更日志时推送代码。
- 不得将 `.env`、数据库、备份、API Key、Bot Token 或支付密钥提交到仓库。
